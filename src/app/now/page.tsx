import { NowContent } from "@/components/now-content"

export const metadata = {
  title: "now",
  description: "What Victor Hugo is up to right now.",
}

export default function NowPage() {
  return <NowContent />
}
